# URL do backend (browser). Em produção: https://api.seudominio.com
NEXT_PUBLIC_API_URL="http://localhost:4000"
# URL do backend para SSR (server→server). Pode ser a mesma da pública.
API_INTERNAL_URL="http://localhost:4000"
# Segredo dos JWT — o MESMO do backend (aqui só verifica o cookie).
JWT_SECRET="troque-por-um-segredo-longo-e-aleatorio"
